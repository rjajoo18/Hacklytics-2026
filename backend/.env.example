# Copy this file to .env or add these to the project-root .env.local
# The backend reads .env.local first, then .env, walking up from backend/app/core/

# Option A: use the service-role key for full server-side DB access (bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Option B: anon key (same as used by the Next.js frontend)
# SUPABASE_ANON_KEY=your-anon-key-here

# Supabase project URL (can also be set as NEXT_PUBLIC_SUPABASE_URL)
SUPABASE_URL=https://your-project.supabase.co
